====================================================================
TC2038 - Integrated Activity 1.2: Transmission Analysis Tool
Team Members & Task Distribution
====================================================================

Joint Collaboration (All Stages):
  - Jointly analyzed, planned, and designed the overall problem-solving 
    strategy for each module (Parts 1, 2, and 3) prior to implementation.
  - Formulated the algorithm flow, state transitions, and edge case handling.
  - Executed end-to-end integration testing, output validation against test cases, 
    and code optimization to adhere to C++ guidelines.

--------------------------------------------------------------------

1. Angela Andrea Castro García (A01640019)
   - Responsibilities:
     * Stage #1 (Part 1 - Malicious Code Search):
       - Implemented Knuth-Morris-Pratt (KMP) algorithm in `findMaliciousCodePosition`.
       - Built `computeLps` function to construct the Longest Proper Prefix 
         which is also Suffix (LPS) array for pattern matching optimization.
     * Stage #3 (Part 3 - Longest Common Substring):
       - Developed `findLongestCommonSubstring` utilizing a 2D Dynamic Programming 
         (DP) table to locate matching hex sequences.
       - Implemented 1-based index calculation mapped relative to transmission1.txt.

2. Andrea Villaseñor Jiménez (A01642088)
   - Responsibilities:
     * Stage #2 (Part 2 - Longest Palindromic Substring):
       - Implemented Manacher's Algorithm in `longestPalindrome` to achieve O(N) 
         linear time complexity for searching mirrored hex patterns.
       - Built string transformation logic using '#' spacers (`temp_string`) to 
         unify odd and even length palindrome evaluations.
       - Developed coordinate translation math `(center_index - max_length) / 2` 
         and adjusted outputs to meet 1-based indexing specifications (+1).
     * Data Ingestion & File I/O Setup:
       - Designed and integrated `readFile` stream logic in `main.cpp` using `ifstream` 
         and `getline` to load and sanitize all text files (`transmission1.txt`, 
         `transmission2.txt`, `mcode1.txt`, `mcode2.txt`, `mcode3.txt`).
       - Implemented CR/LF line-ending removal (`\r`/`\n`) to prevent buffer corruption.
     * Repository Setup & Git Management:
       - Created, initialized, and structured the GitHub repository architecture.
       - Managed branch flow (stage branches), Pull Requests (PRs), code reviews, 
         merge conflicts, and overall repository maintenance.

====================================================================
Date: September 11, 2026
====================================================================

NOTE: team.txt was assited with Gemini AI (for a better format and readability)